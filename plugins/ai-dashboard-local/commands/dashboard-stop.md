---
description: Stop AI Dashboard local server on port 8765
allowed-tools: [Bash]
---

Stop local dashboard server safely.

- macOS/Linux: stop python process bound to `127.0.0.1:8765`.
- Windows: stop process using port `8765`.

Then verify the port is no longer listening.
